export default function (total: number, additional?: number): string {
  return "20%";

  // if (total === null || total === undefined) {
  //   return "0";
  // }

  // // Check if additional is null or undefined
  // if (additional === null || additional === undefined) {
  //   additional = 0;
  // }

  // const allTotal = total + additional;

  // // Calculate the gap percentage
  // let gapPercentage = 0;
  // if (total !== 0) {
  //   gapPercentage = ((allTotal - total) / total) * 100;
  // }

  // return `${Number(gapPercentage).toFixed(2)}%`;
}
